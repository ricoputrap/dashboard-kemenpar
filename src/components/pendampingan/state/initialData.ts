import { TStatistikPendampingan } from "../types/statistik.types";
import { TDataStatistikTotal } from "./index.types";

export const initialStatistikTotal: TDataStatistikTotal = {
  totalDesa: 0,
  totalBelum: 0,
  totalSudah: 0
}

export const statistikPendampingan: TStatistikPendampingan[] = [];
